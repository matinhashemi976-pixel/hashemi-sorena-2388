<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=0.5">
<meta name="description" content="فروشگاه میر جلیل هاشمی - عمده فروشی و خرده فروشی مواد غذایی، برنج، حبوبات و آجیل با بهترین قیمت و کیفیت">
<title>فروشگاه میر جلیل هاشمی</title>

<link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v33.003/Vazirmatn-font-face.css" rel="stylesheet">

<style>

:root{
  --c-red:#d1342b;
  --c-red-dark:#a8241c;
  --c-gold:#c9952c;
  --c-gold-dark:#a8791f;
  --c-cream:#faf6ef;
  --c-ink:#22262f;
  --c-white:#ffffff;
  --c-green:#2e7d32;
  --c-blue:#1565c0;
  --radius-lg:26px;
  --radius-md:18px;
  --shadow-soft:0 12px 30px rgba(0,0,0,.07);
  --shadow-strong:0 18px 40px rgba(0,0,0,.14);
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:Vazirmatn,sans-serif;
  scroll-behavior:smooth;
}

body{
  background:linear-gradient(180deg,var(--c-cream),#f1ece1);
  color:var(--c-ink);
  overflow-x:hidden;
}

a:focus-visible,
button:focus-visible{
  outline:3px solid var(--c-gold);
  outline-offset:3px;
  border-radius:8px;
}

/* ---------- نوار تبلیغاتی بالای سایت ---------- */

.ad-strip{
  background:linear-gradient(90deg,var(--c-gold-dark),var(--c-gold));
  color:#231a06;
  text-align:center;
  font-weight:700;
  font-size:14px;
  padding:9px 14px;
  letter-spacing:.2px;
}

.ad-strip span{
  display:inline-block;
  transition:opacity .4s ease;
}

/* ---------- هدر ---------- */

header{
  background:linear-gradient(135deg,#ffffff,#f5f7fb);
  padding:40px 20px;
  text-align:center;
  border-bottom:1px solid #e8e8e8;
  box-shadow:var(--shadow-soft);
  position:relative;
}

.logo{
  font-size:42px;
  font-weight:900;
  color:var(--c-red);
  letter-spacing:-1px;
  display:inline-block;
  animation:logoPulse 4s ease-in-out infinite;
}

@keyframes logoPulse{
  0%,100%{color:var(--c-red);}
  50%{color:var(--c-gold-dark);}
}

.slogan{
  margin-top:12px;
  font-size:17px;
  color:#666;
}

.contact-bar{
  margin-top:18px;
  display:flex;
  justify-content:center;
  flex-wrap:wrap;
  gap:14px;
}

.contact-bar a{
  text-decoration:none;
  font-weight:700;
  font-size:14px;
  color:#fff;
  background:var(--c-red);
  padding:10px 18px;
  border-radius:30px;
  transition:.25s;
  box-shadow:var(--shadow-soft);
}

.contact-bar a:hover{
  background:var(--c-red-dark);
  transform:translateY(-2px);
}

/* ---------- ناوبری ---------- */

nav{
  width:95%;
  margin:15px auto;
  background:rgba(255,255,255,.92);
  backdrop-filter:blur(15px);
  display:flex;
  justify-content:center;
  flex-wrap:wrap;
  gap:12px;
  padding:15px;
  border-radius:20px;
  position:sticky;
  top:10px;
  z-index:500;
  box-shadow:var(--shadow-soft);
}

nav a{
  text-decoration:none;
  color:#333;
  font-weight:700;
  padding:12px 20px;
  border-radius:12px;
  transition:.35s;
}

nav a:hover{
  background:var(--c-red);
  color:#fff;
  transform:translateY(-3px);
}

#soon-badge{
  background:#ff3d00;
  color:#fff;
  font-size:11px;
  padding:4px 8px;
  border-radius:30px;
  margin-right:5px;
  animation:blink 1.4s infinite;
}

@keyframes blink{
  50%{opacity:.4;}
}

/* ---------- اسلایدر (مشترک برای همه اسلایدرها) ---------- */

.slider{
  width:95%;
  height:460px;
  margin:25px auto;
  background:#fff;
  border-radius:var(--radius-lg);
  overflow:hidden;
  position:relative;
  box-shadow:var(--shadow-soft);
}

.slide{
  position:absolute;
  inset:0;
  opacity:0;
  transition:opacity 1s ease;
}

.slide.active{
  opacity:1;
}

.slide img{
  width:100%;
  height:100%;
  object-fit:contain;
  background:#fff;
}

.slider-dots{
  position:absolute;
  bottom:16px;
  left:50%;
  transform:translateX(-50%);
  display:flex;
  gap:8px;
  z-index:5;
}

.slider-dots button{
  width:10px;
  height:10px;
  border-radius:50%;
  border:none;
  background:rgba(0,0,0,.25);
  cursor:pointer;
  padding:0;
  transition:.3s;
}

.slider-dots button.active{
  background:var(--c-red);
  width:26px;
  border-radius:6px;
}

.slider-arrow{
  position:absolute;
  top:50%;
  transform:translateY(-50%);
  background:rgba(255,255,255,.85);
  border:none;
  width:42px;
  height:42px;
  border-radius:50%;
  font-size:18px;
  cursor:pointer;
  box-shadow:var(--shadow-soft);
  z-index:5;
  transition:.25s;
}

.slider-arrow:hover{
  background:var(--c-red);
  color:#fff;
}

.slider-arrow.next{ right:16px; }
.slider-arrow.prev{ left:16px; }

/* ---------- نوار اعلان متحرک (تیکر) ---------- */

.promo-ticker{
  width:95%;
  margin:0 auto 25px;
  background:linear-gradient(135deg,var(--c-red),var(--c-red-dark));
  color:#fff;
  border-radius:var(--radius-md);
  padding:16px 26px;
  text-align:center;
  font-weight:800;
  font-size:18px;
  box-shadow:var(--shadow-soft);
  overflow:hidden;
  position:relative;
  min-height:30px;
}

.promo-ticker .ticker-text{
  display:inline-block;
  transition:transform .5s ease, opacity .5s ease;
}

/* ---------- بنرها ---------- */

.amazing{
  width:95%;
  margin:30px auto;
  padding:35px;
  border-radius:var(--radius-lg);
  text-align:center;
  background:linear-gradient(135deg,#ff5252,var(--c-red-dark));
  color:#fff;
  box-shadow:0 15px 35px rgba(213,0,0,.25);
}

.amazing h2{
  margin-bottom:10px;
  font-size:30px;
}

.banner{
  width:95%;
  margin:30px auto;
  padding:40px;
  border-radius:var(--radius-lg);
  text-align:center;
  background:linear-gradient(135deg,var(--c-green),#2e7d32);
  color:#fff;
  box-shadow:0 15px 35px rgba(46,125,50,.25);
}

.banner h2{
  margin-bottom:10px;
  font-size:30px;
}

/* ---------- جای تبلیغات ---------- */

.ad-slot{
  width:95%;
  margin:30px auto;
  padding:30px;
  border-radius:var(--radius-lg);
  text-align:center;
  background:repeating-linear-gradient(135deg,#fffaf0,#fffaf0 10px,#fdf3df 10px,#fdf3df 20px);
  border:2px dashed var(--c-gold-dark);
  position:relative;
  color:#6b5316;
}

.ad-slot .ad-label{
  position:absolute;
  top:-12px;
  right:20px;
  background:var(--c-gold-dark);
  color:#fff;
  font-size:12px;
  font-weight:700;
  padding:4px 12px;
  border-radius:30px;
}

.ad-slot h3{
  font-size:22px;
  margin-bottom:8px;
}

.ad-slot a{
  display:inline-block;
  margin-top:14px;
  padding:10px 22px;
  background:var(--c-gold-dark);
  color:#fff;
  text-decoration:none;
  border-radius:10px;
  font-weight:700;
  transition:.3s;
}

.ad-slot a:hover{
  background:var(--c-ink);
}

.ad-card{
  background:linear-gradient(135deg,#fff7e6,#fdecc8);
  border:2px dashed var(--c-gold-dark);
  border-radius:20px;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  text-align:center;
  padding:20px;
  height:240px;
  color:#6b5316;
}

.ad-card span.tag{
  font-size:11px;
  font-weight:700;
  background:var(--c-gold-dark);
  color:#fff;
  padding:3px 10px;
  border-radius:20px;
  margin-bottom:10px;
}

/* ---------- عنوان بخش ---------- */

.section-title{
  text-align:center;
  margin:45px 0 25px;
  font-size:34px;
  font-weight:900;
  color:var(--c-red);
  position:relative;
}

.section-title::after{
  content:"";
  display:block;
  width:80px;
  height:4px;
  background:var(--c-red);
  margin:10px auto;
  border-radius:10px;
}

/* ---------- گالری ---------- */

.gallery{
  width:95%;
  margin:auto;
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:25px;
}

.gallery img{
  width:100%;
  height:240px;
  object-fit:contain;
  background:#fff;
  padding:15px;
  border-radius:20px;
  box-shadow:var(--shadow-soft);
  transition:transform .25s ease, box-shadow .25s ease;
  will-change:transform;
}

.gallery img:hover{
  box-shadow:0 18px 35px rgba(0,0,0,.12);
}

/* ---------- باکس محصولات (دیجی‌کالایی) ---------- */

.products-container{
  width:95%;
  margin:45px auto;
  background:#fff;
  padding:30px;
  border-radius:var(--radius-lg);
  box-shadow:var(--shadow-soft);
}

.products-title{
  text-align:center;
  margin-bottom:30px;
  font-size:28px;
  font-weight:900;
  color:var(--c-red);
}

.products-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(200px,1fr));
  gap:20px;
}

.product-card{
  background:#fff;
  border:1px solid #e0e0e0;
  border-radius:12px;
  padding:15px;
  text-align:center;
  transition:all .3s ease;
  cursor:pointer;
  position:relative;
}

.product-card:hover{
  transform:translateY(-8px);
  box-shadow:0 8px 20px rgba(0,0,0,.15);
  border-color:var(--c-red);
}

.product-icon{
  font-size:4rem;
  margin-bottom:10px;
  display:block;
}

.product-name{
  font-weight:700;
  font-size:15px;
  color:var(--c-ink);
  margin-bottom:8px;
  min-height:2.5em;
  line-height:1.3;
}

.product-price{
  font-size:18px;
  font-weight:900;
  color:var(--c-red);
  margin-bottom:8px;
}

.product-original{
  font-size:13px;
  color:#999;
  text-decoration:line-through;
  margin-bottom:10px;
}

.product-discount{
  display:inline-block;
  background:var(--c-red);
  color:#fff;
  padding:5px 12px;
  border-radius:20px;
  font-size:12px;
  font-weight:700;
  margin-bottom:12px;
}

.product-rating{
  font-size:12px;
  color:#ff9800;
  margin-bottom:12px;
}

.product-btn{
  width:100%;
  padding:10px;
  background:var(--c-red);
  color:#fff;
  border:none;
  border-radius:8px;
  font-weight:700;
  cursor:pointer;
  transition:.3s;
}

.product-btn:hover{
  background:var(--c-red-dark);
  transform:scale(1.02);
}

/* ---------- درباره ما ---------- */

.about{
  width:95%;
  margin:40px auto;
  background:#fff;
  padding:35px;
  border-radius:var(--radius-lg);
  line-height:2.3;
  box-shadow:var(--shadow-soft);
}

.about h2{
  margin-bottom:15px;
  color:var(--c-red);
}

/* ---------- روبیکا ---------- */

.rubika{
  width:95%;
  margin:35px auto;
  background:linear-gradient(135deg,var(--c-blue),#1565c0);
  padding:40px;
  border-radius:var(--radius-lg);
  text-align:center;
  color:#fff;
  box-shadow:0 15px 35px rgba(21,101,192,.25);
}

.rubika h2{
  margin-bottom:10px;
}

.rubika a{
  display:inline-block;
  margin-top:18px;
  padding:14px 28px;
  background:#fff;
  color:var(--c-blue);
  text-decoration:none;
  border-radius:12px;
  font-weight:bold;
  transition:.3s;
}

.rubika a:hover{
  transform:scale(1.06);
}

.map-wrap{
  width:95%;
  margin:0 auto 30px;
  border-radius:var(--radius-lg);
  overflow:hidden;
  box-shadow:var(--shadow-soft);
  height:380px;
}

.map-wrap iframe{
  width:100%;
  height:100%;
  border:0;
}

/* ---------- فوتر ---------- */

footer{
  background:#fff;
  margin-top:60px;
  padding:60px 20px 100px;
  border-top:1px solid #e5e7eb;
}

.footer-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
  gap:25px;
  max-width:1300px;
  margin:auto;
}

.footer-box{
  background:#f8fafc;
  padding:25px;
  border-radius:20px;
  box-shadow:0 5px 15px rgba(0,0,0,.05);
  transition:.3s;
}

.footer-box:hover{
  transform:translateY(-5px);
}

.footer-box h3{
  color:var(--c-red);
  margin-bottom:15px;
}

.footer-box.ad{
  background:linear-gradient(135deg,#fff7e6,#fdecc8);
  border:2px dashed var(--c-gold-dark);
}

.footer-box.ad h3{
  color:var(--c-gold-dark);
}

.copy{
  text-align:center;
  margin-top:35px;
  padding-top:20px;
  border-top:1px solid #ddd;
  color:#666;
  font-size:14px;
}

/* ---------- ابزارهای شناور ---------- */

.float-btn{
  position:fixed;
  z-index:9999;
  border:none;
  cursor:pointer;
  box-shadow:var(--shadow-strong);
}

#top-btn{
  bottom:90px;
  left:20px;
  width:54px;
  height:54px;
  border-radius:50%;
  background:var(--c-red);
  color:#fff;
  font-size:22px;
  display:none;
}

#live-clock{
  position:fixed;
  top:15px;
  left:15px;
  background:#fff;
  padding:9px 14px;
  border-radius:12px;
  font-weight:bold;
  font-size:13px;
  box-shadow:var(--shadow-soft);
  z-index:400;
}

#welcome-toast{
  position:fixed;
  top:70px;
  right:20px;
  background:#fff;
  padding:15px 25px;
  border-radius:15px;
  box-shadow:var(--shadow-soft);
  z-index:9999;
  max-width:80vw;
}

#countdown-badge{
  position:fixed;
  bottom:90px;
  right:20px;
  background:var(--c-red);
  color:#fff;
  padding:12px 16px;
  border-radius:15px;
  font-weight:bold;
  font-size:14px;
  z-index:400;
  box-shadow:var(--shadow-soft);
}

/* ---------- نوار تبلیغاتی چسبان پایین ---------- */

#sticky-ad-bar{
  position:fixed;
  bottom:0;
  right:0;
  left:0;
  background:linear-gradient(90deg,var(--c-ink),#343b4a);
  color:#fff;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:18px;
  padding:14px 20px;
  font-weight:700;
  font-size:14px;
  z-index:9000;
  box-shadow:0 -8px 25px rgba(0,0,0,.2);
}

#sticky-ad-bar .ad-tag{
  background:var(--c-gold);
  color:#231a06;
  font-size:11px;
  padding:3px 10px;
  border-radius:20px;
  flex-shrink:0;
}

#sticky-ad-bar button{
  background:none;
  border:none;
  color:#fff;
  font-size:18px;
  cursor:pointer;
  flex-shrink:0;
}

/* ---------- مودال محصول ---------- */

.product-modal{
  display:none;
  position:fixed;
  top:0;
  left:0;
  right:0;
  bottom:0;
  background:rgba(0,0,0,.6);
  z-index:10000;
  align-items:center;
  justify-content:center;
}

.product-modal.active{
  display:flex;
}

.modal-content{
  background:#fff;
  padding:30px;
  border-radius:20px;
  max-width:600px;
  width:90%;
  max-height:90vh;
  overflow-y:auto;
  box-shadow:var(--shadow-strong);
  animation:slideIn .4s ease;
}

@keyframes slideIn{
  from{
    opacity:0;
    transform:translateY(-50px);
  }
  to{
    opacity:1;
    transform:translateY(0);
  }
}

.modal-header{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom:20px;
}

.modal-close{
  background:none;
  border:none;
  font-size:28px;
  cursor:pointer;
  color:var(--c-red);
}

.modal-icon{
  font-size:5rem;
  text-align:center;
  margin:20px 0;
}

/* ---------- فیلتر و جستجو محصولات ---------- */

.search-filter{
  width:95%;
  margin:20px auto;
  display:flex;
  gap:15px;
  flex-wrap:wrap;
  justify-content:center;
}

.search-filter input,
.search-filter select{
  padding:10px 15px;
  border:1px solid #ddd;
  border-radius:8px;
  font-size:14px;
}

.search-filter input{
  flex:1;
  min-width:200px;
}

::-webkit-scrollbar{ width:10px; }
::-webkit-scrollbar-thumb{ background:var(--c-red); border-radius:20px; }
::-webkit-scrollbar-track{ background:#eee; }

@media(prefers-reduced-motion: reduce){
  *{ animation:none !important; transition:none !important; }
}

@media(max-width:768px){
  .logo{ font-size:28px; }
  .slogan{ font-size:14px; }
  .slider{ height:260px; }
  .section-title{ font-size:26px; }
  nav{ gap:8px; }
  nav a{ padding:10px 12px; font-size:14px; }
  .about,.banner,.amazing,.rubika,.ad-slot{ padding:25px; }
  .promo-ticker{ font-size:14px; padding:12px 16px; }
  #sticky-ad-bar{ font-size:12px; padding:10px 12px; }
  .map-wrap{ height:260px; }
  .contact-bar a{ font-size:13px; padding:8px 14px; }
  .products-grid{ grid-template-columns:repeat(auto-fill,minmax(150px,1fr)); }
}

</style>
</head>

<body>

<!-- جای تبلیغات: نوار باریک بالای سایت - چرخشی -->
<div class="ad-strip" id="topAdStrip">
<span class="ad-strip-text">برای رزرو فضای تبلیغاتی با ما تماس بگیرید</span>
</div>

<header>
<div class="logo">فروشگاه میر جلیل هاشمی </div>
<div class="slogan">عمده فروشی و خرده فروشی مواد غذایی با بهترین تخفیف ها</div>
<div class="contact-bar">
<a href="tel:+984533238235">📞 ۰۴۵-۳۳۲۳۸۲۳۵</a>
<a href="https://www.google.com/maps/place/Matin+Hashemi+Store/@38.2454493,48.3019995,17z" target="_blank" rel="noopener">📍 مسیر فروشگاه روی نقشه</a>
</div>
</header>

<nav>
<a href="#">خانه</a>
<a href="fif 2.HTML">تخفیف ها</a>
<a href="#">دسته بندی ها</a>
<a href="kala-barg.html">کالابرگ</a>
<a href="#">گالری</a>
<a href="ozv2026/panel-ozv.html">پرسنل کارکنان فروشگاه</a>
<a href="#" id="ajilLink">🥜 آجیل عید <span id="soon-badge">به زودی</span></a>
<a href="ARTEBAT BA MA.HTML">تماس با ما</a>
</nav>

<!-- اسلایدر اول -->
<div class="slider" data-autoplay="4500">
  <div class="slide active"><img src="kala barg slaid tir 1405.png" alt="پیشنهاد کالابرگ خردادماه"></div>
  <div class="slide"><img src="hed kol.png" alt="تخفیف ویژه فروشگاه"></div>
  <div class="slide"><img src="taitel.png" alt="محصولات پگاه"></div>
  <div class="slide"><img src="poshtibani - slaid.png" alt="قند و شکر"></div>
  <div class="slide"><img src="kand2.jpg" alt="قند و شکر بسته‌بندی"></div>
</div>

<!-- تیکر تبلیغاتی متحرک (جایگزین بنر پاپ‌آپ قبلی) -->
<div class="promo-ticker">
<span class="ticker-text" id="tickerText">🌹 به فروشگاه میر جلیل هاشمی خوش آمدید</span>
</div>

<div class="amazing">
<h2>🔥 خبر فوری خرید کالابرگ 🔥</h2>
<p>شروع دوره جدید خرید کالابرگ (۱۵ روز خرداد)</p>
</div>

<div class="banner">
<h2>🛒 خرید با کالابرگ الکترونیکی</h2>
<p>مشتریان عزیز می‌توانند با استفاده از کالابرگ الکترونیکی کالاهای مورد نیاز خود را از فروشگاه تهیه کنند.</p>
</div>

<!-- جای تبلیغات: بنر بزرگ وسط صفحه -->
<div class="ad-slot">
<span class="ad-label">تبلیغات</span>
<h3>فضای تبلیغاتی شما اینجاست</h3>
<p>برای معرفی محصول یا برند خود در این بخش با ما در ارتباط باشید.</p>
<a href="ertebat-ba-ma.html">تماس برای تبلیغ</a>
</div>

<h2 class="section-title">گالری فروشگاه</h2>

<div class="gallery" id="galleryGrid">
  <img src="galeri 1.jpg" alt="نمای فروشگاه ۱">
  <img src="galeri 2.jpg" alt="نمای فروشگاه ۲">
  <img src="galeri 3.jpg" alt="نمای فروشگاه ۳">
  <img src="galeri 4.jpg" alt="نمای فروشگاه ۴">
  <!-- جای تبلیغات داخل گالری -->
  <div class="ad-card">
    <span class="tag">تبلیغات</span>
    <strong>برند شما می‌تواند اینجا باشد</strong>
    <p style="margin-top:8px;font-size:13px;">جهت رزرو این جایگاه تماس بگیرید</p>
  </div>
</div>

<!-- جستجو و فیلتر محصولات -->
<div class="search-filter">
<input type="text" id="productSearch" placeholder="جستجو محصولات...">
<select id="categoryFilter">
<option value="">همه دسته‌بندی‌ها</option>
<option value="برنج">برنج</option>
<option value="حبوبات">حبوبات</option>
<option value="آجیل">آجیل</option>
<option value="شکر">شکر</option>
<option value="دیگر">دیگر</option>
</select>
</div>

<!-- باکس محصولات (دیجی‌کالایی) -->
<div class="products-container">
<h2 class="products-title">✨ محصولات پیشنهادی</h2>
<div class="products-grid" id="productsGrid"></div>
</div>

<!-- اسلایدر دوم -->
<div class="slider" data-autoplay="5000">
  <div class="slide active"><img src="kala barg slaid tir 1405.png" alt="محصول شماره یک"></div>
  <div class="slide"><img src="hed kol.png" alt="تخفیف ویژه فروشگاه"></div>
  <div class="slide"><img src="kala-barg.jpg" alt="کالابرگ الکترونیکی"></div>
  <div class="slide"><img src="kand.jpg" alt="قند و شکر"></div>
  <div class="slide"><img src="kand2.jpg" alt="قند و شکر بسته‌بندی"></div>
</div>

<div class="about">
<h2>درباره فروشگاه</h2>
<p>فروشگاه میر جلیل هاشمی با سال‌ها سابقه در زمینه فروش عمده و خرده مواد غذایی، برنج، حبوبات، آجیل و تنقلات فعالیت می‌کند و همواره تلاش کرده است بهترین قیمت و کیفیت را به مشتریان ارائه دهد.</p>
</div>

<!-- جای تبلیغات قبل از روبیکا -->
<div class="ad-slot">
<span class="ad-label">تبلیغات</span>
<h3>همکاری تجاری با فروشگاه</h3>
<p>اگر تأمین‌کننده یا برند فعال در حوزه مواد غذایی هستید، می‌توانید محصول خود را در این صفحه معرفی کنید.</p>
<a href="ertebat-ba-ma.html">اطلاعات بیشتر</a>
</div>

<div class="rubika">
<h2>کانال روبیکا فروشگاه</h2>
<p>اطلاع از جدیدترین تخفیف‌ها و محصولات</p>
<a href="https://rubika.ir/hashemistorsorena">ورود به کانال روبیکا</a>
</div>

<h2 class="section-title">موقعیت فروشگاه روی نقشه</h2>

<div class="map-wrap">
<iframe
  src="https://www.google.com/maps?q=38.2454493,48.3019995&hl=fa&z=16&output=embed"
  loading="lazy"
  referrerpolicy="no-referrer-when-downgrade"
  title="نقشه فروشگاه میر جلیل هاشمی">
</iframe>
</div>

<footer>
<div class="footer-grid">

<div class="footer-box">
<h3>درباره ما</h3>
<p>مرجع تخفیف‌های فروشگاه میر جلیل هاشمی</p>
</div>

<div class="footer-box">
<h3>خدمات</h3>
<p>فروش عمده<br>فروش خرده<br>پذیرش کالابرگ<br>ارسال سفارش</p>
</div>

<div class="footer-box">
<h3>ساعات کاری</h3>
<p>همه روزه ۸ صبح تا ۱۱ شب</p>
</div>

<div class="footer-box">
<h3>ارتباط با ما</h3>
<p>
تلفن: <a href="tel:+984533238235" style="color:var(--c-red);font-weight:700;">۰۴۵-۳۳۲۳۸۲۳۵</a><br>
آدرس: <a href="https://www.google.com/maps/place/Matin+Hashemi+Store/@38.2454493,48.3019995,17z" target="_blank" rel="noopener" style="color:var(--c-red);font-weight:700;">مشاهده در نقشه</a><br>
روبیکا: <a href="https://rubika.ir/hashemistorsorena" style="color:var(--c-red);font-weight:700;">ورود به کانال</a>
</p>
</div>

<div class="footer-box ad">
<h3>تبلیغات</h3>
<p>این جایگاه برای معرفی کسب‌وکارهای همکار در دسترس است.</p>
<a href="ertebat-ba-ma.html" style="color:#a8791f;font-weight:700;">رزرو این فضا ←</a>
</div>

</div>

<div class="copy">
© تمامی حقوق متعلق به فروشگاه میر جلیل هاشمی می‌باشد.
<span id="visitCount" style="display:block;margin-top:6px;font-size:12px;color:#999;"></span>
</div>
</footer>

<!-- ابزارهای شناور -->
<button id="top-btn" class="float-btn" aria-label="بازگشت به بالا">⬆</button>
<div id="live-clock" aria-hidden="true"></div>
<div id="countdown-badge"></div>

<!-- نوار تبلیغاتی چسبان پایین صفحه -->
<div id="sticky-ad-bar">
<span class="ad-tag">تبلیغات</span>
<span id="stickyAdText">🥜 آجیل و خشکبار عید را از همین حالا رزرو کنید</span>
<button id="closeStickyAd" aria-label="بستن تبلیغ">✕</button>
</div>

<!-- مودال محصول -->
<div class="product-modal" id="productModal">
<div class="modal-content">
<div class="modal-header">
<h2 id="modalProductName"></h2>
<button class="modal-close" onclick="closeProductModal()">✕</button>
</div>
<div class="modal-icon" id="modalProductIcon"></div>
<div style="text-align:center;">
<p style="margin-bottom:15px;"><strong id="modalProductDesc"></strong></p>
<p style="font-size:14px;color:#666;margin-bottom:20px;">دسته: <span id="modalProductCategory" style="color:var(--c-red);font-weight:700;"></span></p>
<p style="font-size:24px;font-weight:900;color:var(--c-red);margin-bottom:10px;">
<span id="modalProductPrice"></span> تومان
</p>
<p style="font-size:13px;color:#999;text-decoration:line-through;margin-bottom:15px;">
<span id="modalProductOriginal"></span> تومان
</p>
<p style="font-size:16px;color:var(--c-red);font-weight:700;margin-bottom:20px;">
تخفیف: <span id="modalProductDiscount"></span>%
</p>
<p style="background:#f5f5f5;padding:15px;border-radius:8px;margin-bottom:15px;color:#555;">
⭐ امتیاز: <span id="modalProductRating"></span>/5
</p>
<button class="product-btn" onclick="addToCart()">افزودن به سبد</button>
</div>
</div>
</div>

<script>

/* ========== داده‌های محصولات ========== */

const allProducts = [
  {
    id: 1,
    name: 'برنج ایرانی درجه یک',
    price: 120000,
    original: 160000,
    icon: '🍚',
    discount: 25,
    rating: 4.8,
    category: 'برنج',
    desc: 'برنج خوش‌عطر و لذیذ ایرانی'
  },
  {
    id: 2,
    name: 'لوبیا سیاه',
    price: 45000,
    original: 60000,
    icon: '🫘',
    discount: 25,
    rating: 4.6,
    category: 'حبوبات',
    desc: 'لوبیا سیاه درجه یک'
  },
  {
    id: 3,
    name: 'نخود زعفرانی',
    price: 55000,
    original: 75000,
    icon: '🥜',
    discount: 27,
    rating: 4.7,
    category: 'حبوبات',
    desc: 'نخود سفید مرغوب'
  },
  {
    id: 4,
    name: 'بادام و فندق',
    price: 180000,
    original: 240000,
    icon: '🌰',
    discount: 25,
    rating: 4.9,
    category: 'آجیل',
    desc: 'آجیل درجه یک با کیفیت بالا'
  },
  {
    id: 5,
    name: 'شکر قند سفید',
    price: 95000,
    original: 130000,
    icon: '🍬',
    discount: 27,
    rating: 4.5,
    category: 'شکر',
    desc: 'شکر سفید درشت دانه'
  },
  {
    id: 6,
    name: 'شکر قند بسته‌بندی',
    price: 85000,
    original: 115000,
    icon: '🎁',
    discount: 26,
    rating: 4.6,
    category: 'شکر',
    desc: 'شکر درون بسته‌های مناسب'
  },
  {
    id: 7,
    name: 'زعفران سرگل',
    price: 250000,
    original: 350000,
    icon: '🌸',
    discount: 28,
    rating: 4.9,
    category: 'دیگر',
    desc: 'زعفران خوش‌رنگ ایرانی'
  },
  {
    id: 8,
    name: 'خاک‌شیرینی',
    price: 35000,
    original: 50000,
    icon: '🥖',
    discount: 30,
    rating: 4.4,
    category: 'دیگر',
    desc: 'خاک‌شیرینی معطر'
  },
  {
    id: 9,
    name: 'عدس قرمز',
    price: 65000,
    original: 85000,
    icon: '🥣',
    discount: 24,
    rating: 4.7,
    category: 'حبوبات',
    desc: 'عدس قرمز درجه یک'
  },
  {
    id: 10,
    name: 'مخلوط آجیل',
    price: 165000,
    original: 220000,
    icon: '🎂',
    discount: 25,
    rating: 4.8,
    category: 'آجیل',
    desc: 'مخلوط آجیل متنوع'
  },
  {
    id: 11,
    name: 'گردو درجه یک',
    price: 200000,
    original: 270000,
    icon: '🥜',
    discount: 26,
    rating: 4.9,
    category: 'آجیل',
    desc: 'گردوی خوب و خوش‌طعم'
  },
  {
    id: 12,
    name: 'شاهی معطر',
    price: 75000,
    original: 100000,
    icon: '🍵',
    discount: 25,
    rating: 4.5,
    category: 'دیگر',
    desc: 'شاهی معطر ایرانی'
  }
];

/* ========== نمایش محصولات ========== */

function renderProducts(products = allProducts) {
  const grid = document.getElementById('productsGrid');
  grid.innerHTML = products.map(product => `
    <div class="product-card" onclick="openProductModal(${product.id})">
      <span class="product-icon">${product.icon}</span>
      <div class="product-name">${product.name}</div>
      <div class="product-price">${product.price.toLocaleString()} تومان</div>
      <div class="product-original">${product.original.toLocaleString()}</div>
      <div class="product-discount">%${product.discount}</div>
      <div class="product-rating">⭐ ${product.rating}/5</div>
      <button class="product-btn">مشاهده جزئیات</button>
    </div>
  `).join('');
}

/* ========== فیلتر جستجو و دسته‌بندی ========== */

document.getElementById('productSearch').addEventListener('input', filterProducts);
document.getElementById('categoryFilter').addEventListener('change', filterProducts);

function filterProducts() {
  const searchTerm = document.getElementById('productSearch').value.toLowerCase();
  const category = document.getElementById('categoryFilter').value;
  
  const filtered = allProducts.filter(p => 
    (p.name.includes(searchTerm) || p.desc.includes(searchTerm)) &&
    (!category || p.category === category)
  );
  
  renderProducts(filtered);
}

/* ========== مودال محصول ========== */

function openProductModal(productId) {
  const product = allProducts.find(p => p.id === productId);
  if (!product) return;
  
  document.getElementById('modalProductName').textContent = product.name;
  document.getElementById('modalProductIcon').textContent = product.icon;
  document.getElementById('modalProductDesc').textContent = product.desc;
  document.getElementById('modalProductCategory').textContent = product.category;
  document.getElementById('modalProductPrice').textContent = product.price.toLocaleString();
  document.getElementById('modalProductOriginal').textContent = product.original.toLocaleString();
  document.getElementById('modalProductDiscount').textContent = product.discount;
  document.getElementById('modalProductRating').textContent = product.rating;
  
  document.getElementById('productModal').classList.add('active');
  document.body.style.overflow = 'hidden';
}

function closeProductModal() {
  document.getElementById('productModal').classList.remove('active');
  document.body.style.overflow = 'auto';
}

function addToCart() {
  const productName = document.getElementById('modalProductName').textContent;
  showToast(`✅ ${productName}  خرید به صورت حضوری می باشد `);
  closeProductModal();
}

document.getElementById('productModal').addEventListener('click', (e) => {
  if (e.target.id === 'productModal') closeProductModal();
});

/* ========== اسلایدرهای عمومی (کاروسل) ========== */

function initCarousel(container){
  const slides = Array.from(container.querySelectorAll(".slide"));
  if(slides.length === 0) return;

  let index = slides.findIndex(s => s.classList.contains("active"));
  if(index < 0) index = 0;

  const dotsWrap = document.createElement("div");
  dotsWrap.className = "slider-dots";
  slides.forEach((_, i) => {
    const dot = document.createElement("button");
    dot.setAttribute("aria-label", "اسلاید " + (i + 1));
    if(i === index) dot.classList.add("active");
    dot.addEventListener("click", () => goTo(i));
    dotsWrap.appendChild(dot);
  });
  container.appendChild(dotsWrap);

  const nextBtn = document.createElement("button");
  nextBtn.className = "slider-arrow next";
  nextBtn.setAttribute("aria-label", "اسلاید بعدی");
  nextBtn.innerHTML = "‹";
  container.appendChild(nextBtn);

  const prevBtn = document.createElement("button");
  prevBtn.className = "slider-arrow prev";
  prevBtn.setAttribute("aria-label", "اسلاید قبلی");
  prevBtn.innerHTML = "›";
  container.appendChild(prevBtn);

  function render(){
    slides.forEach((s, i) => s.classList.toggle("active", i === index));
    Array.from(dotsWrap.children).forEach((d, i) => d.classList.toggle("active", i === index));
  }

  function goTo(i){
    index = (i + slides.length) % slides.length;
    render();
  }

  nextBtn.addEventListener("click", () => goTo(index + 1));
  prevBtn.addEventListener("click", () => goTo(index - 1));

  const delay = parseInt(container.dataset.autoplay || "4500", 10);
  let timer = setInterval(() => goTo(index + 1), delay);

  container.addEventListener("mouseenter", () => clearInterval(timer));
  container.addEventListener("mouseleave", () => {
    timer = setInterval(() => goTo(index + 1), delay);
  });

  render();
}

document.querySelectorAll(".slider").forEach(initCarousel);

/* ========== بازگشت به بالا ========== */

const topBtn = document.getElementById("top-btn");
window.addEventListener("scroll", () => {
  topBtn.style.display = window.scrollY > 300 ? "block" : "none";
});
topBtn.addEventListener("click", () => window.scrollTo({ top: 0, behavior: "smooth" }));

/* ========== ساعت زنده ========== */

const clockEl = document.getElementById("live-clock");
setInterval(() => {
  clockEl.textContent = new Date().toLocaleTimeString("fa-IR");
}, 1000);

/* ========== پیام خوش‌آمدگویی ========== */

function showToast(message) {
  const toast = document.createElement("div");
  toast.id = "welcome-toast";
  toast.textContent = message;
  document.body.appendChild(toast);
  setTimeout(() => toast.remove(), 4000);
}

window.addEventListener("load", () => {
  showToast("🌹 به فروشگاه میر جلیل هاشمی خوش آمدید");
});

/* ========== انیمیشن اسکرول ========== */

const observer = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (entry.isIntersecting) {
      entry.target.style.opacity = "1";
      entry.target.style.transform = "translateY(0)";
    }
  });
});

document.querySelectorAll(
  ".gallery img, .gallery .ad-card, .about, .banner, .amazing, .rubika, .footer-box, .ad-slot, .product-card"
).forEach((el) => {
  el.style.opacity = "0";
  el.style.transform = "translateY(50px)";
  el.style.transition = "all .8s ease";
  observer.observe(el);
});

/* ========== افکت کج‌شدن سه‌بعدی روی عکس‌های گالری ========== */

document.querySelectorAll(".gallery img").forEach((img) => {
  img.addEventListener("mousemove", (e) => {
    const rect = img.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width - 0.5;
    const y = (e.clientY - rect.top) / rect.height - 0.5;
    img.style.transform = `translateY(-8px) rotateX(${y * -10}deg) rotateY(${x * 10}deg) scale(1.03)`;
  });
  img.addEventListener("mouseleave", () => {
    img.style.transform = "translateY(0) rotateX(0) rotateY(0) scale(1)";
  });
});

/* ========== شمارش معکوس تخفیف ========== */

const countdownEl = document.getElementById("countdown-badge");
const endDate = new Date("2026-12-31");

function renderCountdown(){
  const diff = endDate - new Date();
  if (diff > 0) {
    const days = Math.floor(diff / 86400000);
    const hours = Math.floor((diff % 86400000) / 3600000);
    countdownEl.textContent = `🔥 پایان تخفیف: ${days} روز و ${hours} ساعت`;
  } else {
    countdownEl.textContent = "🔥 تخفیف ویژه به پایان رسید";
  }
}
renderCountdown();
setInterval(renderCountdown, 60000);

/* ========== تیکر تبلیغاتی متحرک ========== */

const tickerMessages = [
  "🌹 به فروشگاه میر جلیل هاشمی خوش آمدید",
  "🛒 خرید آسان با کالابرگ الکترونیکی",
  "⭐ کیفیت بالا و قیمت مناسب",
  "🥜 عرضه انواع برنج، حبوبات، آجیل و مواد غذایی",
  "🚚 فروش عمده و خرده‌فروشی",
  "💎 رضایت مشتریان اولویت ماست",
  "🎁 جدیدترین تخفیف‌ها را از دست ندهید",
  "🏆 سال‌ها تجربه در خدمت مشتریان"
];

const tickerEl = document.getElementById("tickerText");
let tickerIndex = 0;

setInterval(() => {
  tickerEl.style.opacity = "0";
  tickerEl.style.transform = "translateY(-10px)";
  setTimeout(() => {
    tickerIndex = (tickerIndex + 1) % tickerMessages.length;
    tickerEl.textContent = tickerMessages[tickerIndex];
    tickerEl.style.opacity = "1";
    tickerEl.style.transform = "translateY(0)";
  }, 500);
}, 4500);

/* ========== چرخش متن نوار تبلیغاتی بالا ========== */

const topAdMessages = [
  "برای رزرو فضای تبلیغاتی با ما تماس بگیرید",
  "تبلیغ محصول شما در صفحه اصلی فروشگاه",
  "همکاری تجاری با تأمین‌کنندگان مواد غذایی"
];

const topAdEl = document.querySelector(".ad-strip-text");
let topAdIndex = 0;

setInterval(() => {
  topAdEl.style.opacity = "0";
  setTimeout(() => {
    topAdIndex = (topAdIndex + 1) % topAdMessages.length;
    topAdEl.textContent = topAdMessages[topAdIndex];
    topAdEl.style.opacity = "1";
  }, 400);
}, 5000);

/* ========== عنوان متحرک تب مرورگر ========== */

const tabTitles = [
  "فروشگاه میر جلیل هاشمی",
  "بهترین تخفیف‌ها",
  "خرید با کالابرگ",
  "عمده و خرده فروشی"
];

let tabTitleIndex = 0;
setInterval(() => {
  tabTitleIndex = (tabTitleIndex + 1) % tabTitles.length;
  document.title = tabTitles[tabTitleIndex];
}, 3000);

/* ========== شمارنده بازدید ========== */

let visits = parseInt(localStorage.getItem("store_visits") || "0", 10);
visits++;
localStorage.setItem("store_visits", visits);
document.getElementById("visitCount").textContent = `🔁 شما ${visits} بار از این صفحه بازدید کرده‌اید`;

/* ========== لینک «به زودی» ========== */

document.getElementById("ajilLink").addEventListener("click", (e) => {
  e.preventDefault();
  showToast("🥜 بخش آجیل عید به‌زودی فعال می‌شود");
});

/* ========== نوار تبلیغاتی چسبان پایین ========== */

const stickyAdBar = document.getElementById("sticky-ad-bar");
const stickyAdMessages = [
  "🥜 آجیل و خشکبار عید را از همین حالا رزرو کنید",
  "🛒 کالابرگ الکترونیکی فعال است",
  "📦 ارسال سفارش به سراسر شهر",
  "🎁 تخفیف‌های ویژه محصولات"
];
let stickyAdIndex = 0;

if (sessionStorage.getItem("hide_sticky_ad") === "1") {
  stickyAdBar.style.display = "none";
}

document.getElementById("closeStickyAd").addEventListener("click", () => {
  stickyAdBar.style.display = "none";
  sessionStorage.setItem("hide_sticky_ad", "1");
});

setInterval(() => {
  const textEl = document.getElementById("stickyAdText");
  if(!textEl) return;
  textEl.style.opacity = "0";
  setTimeout(() => {
    stickyAdIndex = (stickyAdIndex + 1) % stickyAdMessages.length;
    textEl.textContent = stickyAdMessages[stickyAdIndex];
    textEl.style.opacity = "1";
  }, 400);
}, 6000);

document.getElementById("stickyAdText").style.transition = "opacity .4s ease";
document.querySelector(".ad-strip-text").style.transition = "opacity .4s ease";
document.getElementById("tickerText").style.transition = "opacity .4s ease, transform .4s ease";

/* ========== بیشتر اسکریپت‌های خفن ========== */

// افکت زمینه متحرک
document.addEventListener('scroll', () => {
  const scrolled = window.scrollY;
  const parallax = document.querySelector('header');
  if (parallax) {
    parallax.style.backgroundPosition = `0 ${scrolled * 0.5}px`;
  }
});

// اعلان موقعیت مکان
if (navigator.geolocation) {
  // navigator.geolocation.getCurrentPosition(position => {
  //   showToast(`📍 موقعیت شما: ${position.coords.latitude.toFixed(2)}, ${position.coords.longitude.toFixed(2)}`);
  // });
}

// کیبورد میانبر
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') closeProductModal();
  if (e.ctrlKey && e.key === '/') {
    e.preventDefault();
    document.getElementById('productSearch').focus();
    showToast('🔍 جستجو فعال شد');
  }
});

// صدای دکمه (اختیاری)
function playSound() {
  const audioContext = new (window.AudioContext || window.webkitAudioContext)();
  const oscillator = audioContext.createOscillator();
  const gainNode = audioContext.createGain();
  
  oscillator.connect(gainNode);
  gainNode.connect(audioContext.destination);
  
  oscillator.frequency.value = 800;
  oscillator.type = 'sine';
  
  gainNode.gain.setValueAtTime(0.1, audioContext.currentTime);
  gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.1);
  
  oscillator.start(audioContext.currentTime);
  oscillator.stop(audioContext.currentTime + 0.1);
}

// اضافه کردن صدا به دکمه‌های محصول
document.addEventListener('click', (e) => {
  if (e.target.classList.contains('product-btn')) {
    playSound();
  }
});

// تشخیص اینترنت
window.addEventListener('online', () => {
  showToast('✅ اینترنت متصل است');
});

window.addEventListener('offline', () => {
  showToast('❌ اینترنت قطع شد');
});

// شمارش پایین به تاریخ شروع سال جدید
setInterval(() => {
  const newYear = new Date('2026-01-01');
  const now = new Date();
  const diff = newYear - now;
  
  if (diff > 0) {
    const days = Math.floor(diff / 86400000);
    const hours = Math.floor((diff % 86400000) / 3600000);
    const minutes = Math.floor((diff % 3600000) / 60000);
    const seconds = Math.floor((diff % 60000) / 1000);
    
    // می‌تونید این رو جایی نمایش دهید
  }
}, 1000);

// تغییر رنگ نوار آدرس مرورگر
const metaThemeColor = document.createElement('meta');
metaThemeColor.name = 'theme-color';
metaThemeColor.content = 'var(--c-red)';
document.head.appendChild(metaThemeColor);

// اولین نمایش محصولات
renderProducts();

</script>

</body>
</html>
